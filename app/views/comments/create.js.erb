$('.new-comment-errors').html('<%= render 'shared/errors', resource: @comment %>');

<% if @comment.persisted? %>
  $('.<%= @comment.commentable_type %>-comments').append('<%= j render @comment %>');
  $('.new-comment #comment_body').val('');
<% end %>
